import socket
end_of_header = "|||"

class SocketTCP:
    def __init__(self):
        socket = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
        direccion_destino = None
        direccion_origen = None
        secuencia = None

    def parse_segment(self, segment):
        
        return parse_seg
    def create_segment(self, parse_seg):

        return segment
    #funciones